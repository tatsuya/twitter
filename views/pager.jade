#pager
  if page.count > 1
    if page.number
      //- a#prev(href="/#{page.number}") Prev
      a#prev(href="/tweets?page=#{page.number}") Prev
    if page.number < page.count - 1
      if page.number
        span &nbsp;&nbsp;
      //- a#next(href="/#{page.number + 2}") Next
      a#next(href="/tweets?page=#{page.number + 2}") Next