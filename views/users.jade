extends layout

block content
  include ./menu.jade

  .container(style="margin-top: 15px; margin-bottom: 15px;")
    .row
      .col-md-3
        if user
          .panel.panel-default
            .panel-heading
              a(href="/users").text-muted @#{user.name}
            .panel-body
              a(href="/users") TWEETS #{count}

      .col-md-6
        ul.list-group
          each entry in entries
            li.list-group-item
              h3= entry.title
              p= entry.body
              p.text-muted @#{entry.username}

      .col-md-3
